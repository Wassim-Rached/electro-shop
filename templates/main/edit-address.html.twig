{% extends 'base.html.twig' %}

{% block title %}Change Address{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center pt-3">Change Address</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}

        <a class="btn btn-danger" href="{{ path('app_product_index') }}">back to list</a>
    </div>

{% endblock %}
